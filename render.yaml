services:
  - type: web
    name: spamshield-ai
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: LIGHTWEIGHT_MODE
        value: "1"
